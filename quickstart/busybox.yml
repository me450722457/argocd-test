apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: busybox
  namespace: kube-system
spec:
  templates:
    containers:
    - name: busybox
      image: busybox:latest
      command:
        - sleep
        - "infinity"
      imagePullPolicy: IfNotPresent
  restartPolicy: Always
